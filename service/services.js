const test = require('./testService');

const user = require('./userService');
const experience = require('./experienceService');
const soft_skill = require('./soft_skillService');
const auth = require('./authService');

module.exports = {
    test,
    user,
    experience,
    soft_skill,
    auth
}